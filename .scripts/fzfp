#!/usr/bin/env bash
# fzfp = fuzzy find project

dirs=(~/projects ~/work ~/personal ~/university)

selected=$(find "${dirs[@]}" -type d -maxdepth 1 | fzf)
if [[ -z $selected ]]; then
    exit 0
fi

echo $selected
